import React from 'react';
import styles from './admins.module.css';

const ModalEditAdmin = ({ boolean }) => {
  if (boolean) {
    return (
      <div>
        <p>a</p>
      </div>
    );
  } else return false;
};

export default ModalEditAdmin;
